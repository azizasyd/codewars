#include <bits/stdc++.h>
using namespace std;

std::string number_to_string(int num) {
    string str = to_string(num);
    return str;
}

int main(){
    int num = 5;
    cout << number_to_string(num) << "\n";
}